# 📊 검색 API (하이브리드 검색) k6 부하 테스트 보고서

[← README로 돌아가기](./README.md)

---

## 📌 테스트 개요 및 목적

본 시스템의 핵심 검색 API(`GET /api/trends/search`)는 **외부 AI API(Gemini) 연동에 따른 토큰 비용과 API Rate Limit, 그리고 일시적 장애 위험**을 동시에 안고 있습니다. 

단일 단위 테스트만으로는 실제 트래픽 유입 시의 안정성을 보장할 수 없으므로, **k6 기반 부하 테스트 환경을 구축**하여 다음 3가지 핵심 목표를 검증했습니다:
1. **비용 통제**: 인기 키워드 검색 폭주 시 외부 API 호출 제한 및 Cache Stampede 방지 여부
2. **동시성 안전**: 동시 요청 유입 시 외부 API Rate Limit 초과 없이 안정적인 순차 처리 여부
3. **장애 복원력**: 외부 AI API 장애 발생 시 사용자 서비스 단절 없는 자동 Fallback 보장 여부

---

## 0. 종합 요약

| 검증 항목 | 결과 |
|---|---|
| Cache Stampede 방지 | 동일 키워드 **50명 동시 요청**에도 외부 AI(Gemini) 임베딩 API **정확히 1회만 호출**, 나머지 49건은 분산락 대기 후 캐시 공유 → 실패율 **0%** |
| 동시성 제어(Semaphore) | 서로 다른 키워드 **20건 동시 캐시 MISS** 상황에서 외부 API 동시 호출을 3개로 제한, 전량 성공(**0% 실패**), p95 **3.35s**(직렬화 대기 반영) |
| Rate Limit(Throttle) | 동일 클라이언트 100건 요청 중 **5건만 허용, 95건 정상 차단(429)** → 설계된 "IP당 10초 5회" 정책이 오차 없이 동작 |
| 하이브리드 검색 성능(순수 DB, Warm) | VU 1→50 램핑 부하에서 **TPS 313 req/s, p95 125.8ms, p99 160.4ms, 실패율 0%** |
| 하이브리드 검색 성능(Cold 종단) | 외부 임베딩 API 호출 포함 실제 Cold 지연 **p95 615.15ms / p99 665.53ms** (캐시 miss 100% 검증) — Redis 임베딩 캐싱이 지연을 약 5배 단축 |
| AI 장애 복원력(FTS 폴백) | 외부 AI API 완전 장애 상황에서도 **가용성 100%** 유지. Early Exit 개선으로 장애 시 지연을 **5.17s → 511.7ms로 약 10배 단축** |
| 검색 방식별 처리량 비교 | 하이브리드(RRF) TPS 313 vs 순수 FTS TPS 621 → 하이브리드가 **약 2배 느리지만** 벡터 기반 의미 검색 제공(정확도-성능 트레이드오프) |

---

## 1. 시나리오 A — Cache Stampede 방지 검증

### 설계 배경 및 이유
동일 키워드에 대량 동시 요청 유입 시 캐시 Miss로 판단되어 외부 API를 중복 호출하는 'Cache Stampede' 현상을 방지하고자, Redis 분산 락과 Polling 대기 로직이 실제 고부하 환경에서 1회의 API 호출만 허용하는지 검증했습니다.

### 수행 내용 및 결과
- 캐시 초기화 후 **동일 키워드("AI")** 로 **50명 동시 요청** 전송 (50 VUs, 각 1회)
- **결과**: 총 50건 전량 성공 (**실패율 0%**), p95 Latency **1.03s**

**서버 로그 기반 검증**:
- `[Embedding Cache MISS]` (외부 API 실호출): **단 1건** (50건 중 실제 Gemini API 호출은 1회)
- `[Embedding Lock Waiting]` (락 대기): **49건** (대기 후 캐시 획득 정상 완료)

---

## 2. 시나리오 B — 동시성 제어(Semaphore(3)) 검증

### 설계 배경 및 이유
서로 다른 키워드 요청이 동시에 몰릴 때 발생할 수 있는 외부 AI API Rate Limit 초과를 방지하기 위해, `Semaphore(3)`로 동시 호출을 3개로 제한할 때 대기열이 에러 없이 작동하는지 확인했습니다.

### 수행 내용 및 결과
- 서로 다른 키워드 20개로 **20명 동시 요청** (20 VUs, 각 1회 캐시 MISS 유발)
- **결과**: 총 20건 전량 성공 (**5xx 0건**), p95 Latency **3.35s** (Semaphore 순차 처리 지연 반영)

---

## 3. 시나리오 B2 — Rate Limit(Throttle) 검증

### 설계 배경 및 이유
악의적인 무차별 요청으로부터 서버 리소스 및 AI 토큰 비용을 보호하는 1차 방어선이 정상 작동하는지 검증했습니다.

### 수행 내용 및 결과
- **동일 IP 클라이언트**가 100건(20명 × 5회)을 짧은 시간에 연속 전송 (`IP당 10초 5회` 제한)
- **결과**: 정상 처리 5건 (5%), **차단(429 Too Many Requests) 95건 (95%)** → 설계값과 정확히 일치

---

## 4. 시나리오 C — 하이브리드 검색 성능 (Cold vs Warm)

### 설계 배경 및 이유
외부 임베딩 API 호출이 포함된 **Cold 종단 지연(C1)** 과 캐시가 적용된 **순수 DB 조회 지연(C2)** 을 분리 측정하여 Redis 임베딩 캐싱의 정량적 성능 이점을 검증했습니다.

<details>
<summary><b>🔍 1차 테스트 오염 감지 및 C1 재정의 과정 (클릭하여 열기)</b></summary>

### 1차 테스트에서 발견한 문제 (C1 지표 왜곡)
- 최초 C1은 22개 고정 키워드로 Ramping 부하(1→50 VU)를 전송하는 방식이었습니다.
- **문제**: 22개 키워드가 극초반에 전부 캐싱되면서 6만 건 이상의 요청이 캐시 HIT으로 전환되었습니다. 그 결과 C1의 p95(126.31ms)가 C2(125.8ms)와 동일해져 Cold 지표가 왜곡되었습니다. (캐시 MISS 비율: ~0.04%)

### C1 재정의 (수정 후)
- **유니크 키워드 30개**(`cold_probe_001`~`030`)를 캐시 초기화 후 단발성 부하로 전송하여 **100% Cold 지연만 순수 실측**했습니다.
</details>

### 결과 비교

| 항목 | C1 (Cold 실측) | C2 (Warm, 순수 DB) |
|---|---|---|
| cache MISS 비율 | **100% (30/30)** | 0% (전부 HIT) |
| 성공률 | **100%** | 100% |
| **p95 Latency** | **615.15ms** | **125.8ms** |
| **p99 Latency** | **665.53ms** | **160.4ms** |
| TPS | 7.11 req/s (Semaphore 제약) | **313.29 req/s** |

- **결론**: Redis 임베딩 캐싱을 통해 지연 시간을 **약 5배 단축** (`615ms` ➔ `125ms`)함을 확인했습니다.

---

## 5. 시나리오 D — AI 장애 시 FTS 자동 폴백 검증

### 설계 배경 및 이유
외부 AI API(Gemini)에 완전 장애가 발생하더라도 사용자 서비스 중단 없이 키워드 검색(FTS)으로 자동 전환되는지 검증했습니다.

<details>
<summary><b>🔍 1차 테스트 5초 지연 원인 분석 및 Early Exit 개선 과정 (클릭하여 열기)</b></summary>

### 1차 테스트에서 발견한 문제
- 장애 시 가용성은 100%였으나, **p95 응답 지연이 5.17초**로 급증했습니다.
- **원인**: 선점 요청(락 보유자)이 장애로 실패했음에도, 대기 요청들이 무조건 **최대 5초(25회 × 200ms) 폴링 대기를 채운 뒤**에야 FTS로 폴백했기 때문입니다.

### 개선 — `waitForCache` Early Exit 도입
- 폴링 루프 내에 `lockKey` 존재 여부를 추가 검증하여, **"락 해제 + 캐시 미생성"** 감지 시 불필요한 대기 없이 즉시 FTS 폴백을 유도했습니다.
</details>

### 개선 전/후 비교

| 항목 | 개선 전 | 개선 후 (Early Exit) |
|---|---|---|
| **가용성 (200)** | **100% (5/5)** | **100% (5/5)** |
| 평균 응답시간 | 4.21s | **467.67ms** |
| **p95 Latency** | **5.17s** | **511.7ms (약 10배 단축)** |

- **결론**: 외부 API 장애 상황에서도 **가용성 100% 보장** 및 Early Exit 도입으로 장애 시 응답 지연을 **약 10배 단축**했습니다.

---

## 6. 시나리오 E — Keyword-only(FTS) 베이스라인

### 설계 배경 및 이유
하이브리드 검색(FTS + pgvector) 도입에 따른 연산 오버헤드를 단순 키워드 검색(FTS)과 비교·정량화하기 위해 수행했습니다.

| 지표 | 순수 FTS (E) | 하이브리드 RRF (C2) | 차이 |
|---|---|---|---|
| p95 Latency | 62.74ms | 125.8ms | 약 2.0배 증가 |
| 최대 TPS | 621 req/s | 313 req/s | 약 50% 감소 |

- **결론**: 하이브리드 검색은 순수 FTS 대비 처리량이 절반 수준이나, 의미론적 연관 문서(Semantic Context) 탐색 가치 대비 허용 가능한 성능 오버헤드 범위임을 검증했습니다.

---

## 7. 종합 지표 테이블

| 시나리오 | 목적 | 부하 | 성공률 | p95 | p99 | TPS | 핵심 성과 |
|---|---|---|---|---|---|---|---|
| **A** | Cache Stampede 방지 | 50 VU 동일 키워드 동시 | 100% | 1.03s | 1.04s | 46.3 | 외부 API 호출 **단 1회로 제한** |
| **B** | Semaphore(3) 동시성 제어 | 20 VU 다른 키워드 동시 | 100% | 3.35s | 3.38s | 5.86 | 동시 호출 **3개 제한**, 무장애 처리 |
| **B2** | Rate Limit(Throttle) | 동일 IP 100건 | 5%(정상) | 191.3ms | 329.29ms | — | **95건 정상 차단(429)** |
| **C1** | 하이브리드 Cold 종단 | 유니크 30건 단발성 | 100% | **615.15ms** | **665.53ms** | 7.11 | 외부 API 포함 실제 Cold 지연 |
| **C2** | 하이브리드 Warm (순수 DB) | Ramp 1→50 VU | 100% | **125.8ms** | **160.4ms** | **313.29** | 기본 인프라 수용량 지표 |
| **D** | AI 장애 FTS 폴백 | 5 VU, Gemini 장애 | 100% | **511.7ms** | 511.93ms | 9.51 | **가용성 100%** + 지연 10배 단축 |
| **E** | FTS 베이스라인 | Ramp 1→50 VU | 100% | 62.74ms | 84.87ms | **621.02** | 하이브리드 대비 성능 비교 기준 |

---