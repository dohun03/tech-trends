# 📊 검색 API (하이브리드 검색) k6 부하 테스트 보고서

[← README로 돌아가기](./README.md)

---

## 📌 테스트 개요 및 목적

본 시스템의 핵심 검색 API(`GET /api/trends/search`)는 **외부 AI API(Gemini) 연동에 따른 토큰 비용과 API Rate Limit, 그리고 일시적 장애 위험**을 동시에 안고 있습니다. 

이러한 특성상 단일 단위 테스트(Jest)만으로는 실제 트래픽 유입 시의 시스템 안정성을 보장할 수 없습니다. 따라서 **k6 기반의 부하 테스트 환경을 구축**하여 다음 3가지 핵심 목표를 검증했습니다:
1. **비용 통제**: 인기 키워드 검색 폭주 시 외부 API 호출 제한 및 Cache Stampede 방지 여부
2. **동시성 안전**: 동시 요청 유입 시 외부 API Rate Limit 초과 없이 안정적인 순차 처리 여부
3. **장애 복원력**: 외부 AI API 장애 발생 시 사용자 서비스 단절 없는 자동 Fallback 보장 여부

---

## 0. 종합 요약

| 검증 항목 | 결과 |
|---|---|
| Cache Stampede 방지 | 동일 키워드 **50명 동시 요청**에도 외부 AI(Gemini) 임베딩 API **정확히 1회만 호출**, 나머지 49건은 분산락 대기 후 캐시 공유 → 실패율 **0%** |
| 동시성 제어(Semaphore) | 서로 다른 키워드 **20건 동시 캐시 MISS** 상황에서 외부 API 동시 호출을 3개로 제한, 전량 성공(**0% 실패**), p95 **3.35s**(직렬화 대기 반영) |
| Rate Limit(Throttle) | 동일 클라이언트 100건 요청 중 **5건만 허용, 95건 정상 차단(429)** → 설계된 "IP당 10초 5회" 정책이 오차 없이 동작 |
| 하이브리드 검색 성능(순수 DB, Warm) | VU 1→50 램핑 부하에서 **TPS 313 req/s, p95 125.8ms, p99 160.4ms, 실패율 0%** |
| 하이브리드 검색 성능(Cold 종단) | 외부 임베딩 API 호출 포함 실제 Cold 지연 **p95 615.15ms / p99 665.53ms** (캐시 miss 100% 검증) — Redis 임베딩 캐싱이 지연을 약 5배 단축 |
| AI 장애 복원력(FTS 폴백) | 외부 AI API 완전 장애 상황에서도 **가용성 100%** 유지. Early Exit 개선으로 장애 시 지연을 **5.17s → 511.7ms로 약 10배 단축** |
| 검색 방식별 처리량 비교 | 하이브리드(RRF) TPS 313 vs 순수 FTS TPS 621 → 하이브리드가 **약 2배 느리지만** 벡터 기반 의미 검색 제공(정확도-성능 트레이드오프) |

---

## 1. 시나리오 A — Cache Stampede 방지 검증

### 설계 배경 및 이유
- **설계 이유**: 동일한 키워드에 대해서 대량의 동시 요청이 들어오면 모든 요청이 캐시 Miss로 판단되어 외부 API를 중복 호출하는 'Cache Stampede' 현상이 발생합니다. 이를 방지하기 위한 Redis 분산 락과 Polling 대기 로직이 실제 고부하 환경에서 1회의 API 호출만 허용하는지 검증하기 위함입니다.

### 수행 내용
- 캐시가 완전히 비워진 상태에서 **동일 키워드("AI")** 로 **50명의 사용자가 동시에** 검색 요청을 보내는 상황을 재현.
- 요청 전 Redis의 임베딩 캐시(`emb:*`)를 초기화
- k6: (50 VUs, 각 1회 동시 실행)

### 결과

| 항목 | 값 |
|---|---|
| 총 요청 수 | 50 |
| 성공(200) | **50 (100%)** |
| 실패(4xx/5xx) | **0 (0%)** |
| p90 Latency | 1.03s |
| **p95 Latency** | **1.03s** |
| **p99 Latency** | **1.04s** |
| 평균 응답시간 | 937.83ms |
| TPS | 46.3 req/s |

**서버 로그 기반 핵심 검증**:

| 로그 항목 | 발생 건수 | 의미 |
|---|---|---|
| `[Embedding Cache MISS]` (외부 API 실호출) | **1건** | 50건의 동시 요청 중 실제 Gemini 임베딩 API는 **단 1회만 호출** |
| `[Embedding Lock Waiting]` (락 대기) | 49건 | 나머지 49건은 분산락 획득 실패 → Polling 대기 진입 |
| `[Embedding Lock Resolved]` (대기 후 캐시 획득) | 49건 | 대기한 49건 전부 캐시 생성을 기다렸다가 정상적으로 결과 획득 |

### 결론
Redis 분산락 + Polling 기반 Cache Stampede 방지 로직이 설계 의도대로 완벽히 동작함을 실측으로 증명. **동일 인기 키워드에 대한 대량 동시 요청 상황에서도 외부 AI API 비용을 1회 호출로 제한**하면서 전체 요청은 실패 없이 처리됨.

---

## 2. 시나리오 B — 동시성 제어(Semaphore(3)) 검증

### 설계 배경 및 이유
- **설계 이유**: 서로 다른 키워드 요청이 동시에 몰릴 경우, 모든 요청이 외부 AI API를 호출하면서 Rate Limit 초과로 인해 전체 서비스 에러가 발생할 수 있습니다. `Semaphore(3)`를 통해 외부 API 동시 호출을 3개로 제한할 때, 나머지 요청들이 에러 없이 대기열에서 안전하게 제어되는지 확인하기 위함입니다.
- **설계 변경 이력**: 초기 설계에서는 VU당 5회 반복 요청을 검토했으나, 2~5차 요청 시 캐시 Hit으로 전환되어 순수 "동시 캐시 Miss 시의 대기 지연 시간" 측정이 오염되는 문제가 있었습니다. 이에 **VU당 1회 실행으로 변경**하여 순수한 동시성 제어 효과만 선명하게 측정했습니다.

### 수행 내용
- 서로 다른 키워드(20개, 각 사용자가 1개씩 담당)로 **20명이 동시에 각 1회씩** 검색 요청 → 20건 모두 캐시 MISS를 유발해 외부 API 호출이 실제로 동시에 몰리는 상황을 재현.
- k6: (20 VUs, 각 1회씩 실행)

### 결과

| 항목 | 값 |
|---|---|
| 총 요청 수 | 20 |
| 성공(200) | **20 (100%)** |
| 실패(4xx/5xx) | **0 (0%)** |
| 최소 응답시간 | 1.34s |
| 평균 응답시간 | 2.24s |
| p90 Latency | 3.07s |
| **p95 Latency** | **3.35s** |
| **p99 Latency** | **3.38s** |
| 최대 응답시간 | 3.39s |
| TPS | 5.86 req/s |

### 결론
`async-mutex` 기반 `Semaphore(3)`이 외부 AI API 동시 호출 수를 정확히 3개로 제한하며 나머지 요청을 대기열에서 순차 처리함을 응답 지연 분포(1.34s ~ 3.39s로 계단형 증가)로 확인. **20건의 동시 부하에도 5xx 없이 전량 성공**했으며, 이는 서버 자체에서 선제적으로 안전장치가 정상 동작함을 의미.

---

## 3. 시나리오 B2 — Rate Limit(Throttle) 검증

### 설계 배경 및 이유
- **설계 이유**: 악의적인 무차별 요청으로로부터 서버 리소스, AI 토큰 비용을 보호하기 위한 1차 방어선이 제대로 닫히는지 확인하는 테스트입니다. 단일 IP 환경을 조성하고, 지정된 임계값 초과 시 정확히 차단되는지 검증했습니다.

### 수행 내용
- **동일 클라이언트(동일 IP)** 가 20명 × 5회 = 100건을 짧은 시간에 반복 요청.
- 검색 라우트 정책: `IP당 10초에 5회` 제한(`@Throttle`).

### 결과

| 항목 | 값 |
|---|---|
| 총 요청 수 | 100 |
| **정상 처리(200)** | **5건 (5%)** |
| **차단(429 Too Many Requests)** | **95건 (95%)** |
| p90 Latency | 180.67ms |
| **p95 Latency** | **191.3ms** |
| **p99 Latency** | **329.29ms** |
| 5xx 서버 오류 | 0건 |

### 결론
"IP당 10초 5회" Rate Limit 정책이 설계값과 **정확히 일치하는 5건만 통과**시키고 나머지 95건을 429로 안전하게 차단함을 확인. 서버 과부하 방지 및 외부 API 비용 급증을 막는 1차 방어선이 정상 동작.

---

## 4. 시나리오 C — 하이브리드 검색 성능 (Cold vs Warm)

### 설계 배경 및 이유
- 하이브리드 검색에서 **외부 임베딩 API 호출이 포함된 실제 Cold 종단 지연(C1)** 과 **캐시 warm 상태의 순수 DB 조회 지연(C2)** 을 분리 측정하여, Redis 임베딩 캐싱이 제공하는 성능 이점을 정량화하기 위함입니다.

### 1차 테스트에서 발견한 문제 (C1 지표 왜곡)
- 최초 C1은 22개 고정 키워드를 Ramping 부하(1→50 VU, 3분 30초)로 61,521건 전송하는 방식이었습니다.
- **문제**: 22개 키워드가 극초반(1~2초)에 전부 임베딩 캐시에 저장되면서, 이후 6만 건 이상의 요청이 전부 캐시 HIT으로 전환되었습니다. 그 결과 C1의 p95(126.31ms)가 순수 DB 조회인 C2(125.8ms)와 사실상 동일해져, "Cold 성능"이라고 보기 어려운 지표 왜곡이 발생했습니다.
- 서버 로그 기준 실제 캐시 MISS 비율은 **22 / 61,521 ≈ 0.04%** 에 불과했습니다.

### C1 재정의 (수정 후)
- **유니크 키워드 30개**(`cold_probe_001`~`030`, 캐시에 절대 존재하지 않는 고유 키워드)를 캐시 초기화 후 **단발성 부하**로 전송.
- executor를 `ramping-vus` → `per-vu-iterations`(VUs=3, Semaphore(3) 정렬 → 불필요한 큐잉 배제)로 변경.
- 각 요청이 서로 다른 키워드를 사용하므로 **모든 요청이 실제 외부 임베딩 API 호출을 유발** → 진짜 Cold 종단 latency 측정.
- 서버 로그로 30건 모두 `[Embedding Cache MISS]`가 발생했음을 검증(cache MISS 비율 **100%**).

### 결과 비교 (수정 전/후)

| 항목 | C1 수정 전(왜곡) | C1 수정 후(Cold 실측) | C2 (Warm, 순수 DB) |
|---|---|---|---|
| 측정 방식 | 22개 키워드 Ramp 3분30초 | 유니크 30건 단발성 | warm-up 후 Ramp |
| 총 요청 수 | 61,521건 | 30건 | 66,127건 |
| cache MISS 비율 | ~0.04% | **100% (30/30)** | 0% (전부 HIT) |
| 성공률 | 100% | **100%** | 100% |
| 평균 응답시간 | 61.28ms | 417.74ms | 56.98ms |
| p90 Latency | 117.15ms | 487.02ms | 114.21ms |
| **p95 Latency** | 126.31ms (왜곡) | **615.15ms** | 125.8ms |
| **p99 Latency** | 156.41ms (왜곡) | **665.53ms** | 160.4ms |
| 최대 응답시간 | 1.1s | 683.14ms | 454.17ms |
| TPS | 292.96 req/s | 7.11 req/s (Semaphore 3 제약) | 313.29 req/s |

### 결론
- **수정 전 C1은 warm 오염으로 "Cold"로 볼 수 없는 126ms 지표**였으나, 재정의 후 실제 외부 임베딩 API 호출을 포함한 Cold 종단 latency는 **p95 615.15ms / p99 665.53ms** 로 정확히 측정되었습니다.
- Cold(615ms) vs Warm(125ms)의 차이(약 5배)가 곧 **Redis 임베딩 캐싱의 실제 성능 기여**를 정량적으로 보여줍니다

---

## 5. 시나리오 D — AI 장애 시 FTS 자동 폴백 검증

### 설계 배경 및 이유
- 외부 AI 서비스(Gemini)에 장애가 발생하더라도 사용자에게 정상 검색 결과를 제공할 수 있는지 검증하는 시나리오입니다. 의도적으로 무효한 API Key를 주입하여 장애 상황을 에뮬레이션했습니다.

### 수행 내용
- `.env`의 `GEMINI_API_KEY`를 의도적으로 무효한 값으로 교체하여 **외부 AI API 완전 장애 상황**을 재현.
- 캐시에 없는 새 키워드로 5 VUs 소규모 요청을 전송해 폴백 경로 트리거.

### 1차 테스트에서 발견한 문제
- 장애 상황에서 가용성은 100%로 유지됐지만, **p95 응답 지연이 5.17초**로 급증했습니다.
- 원인 분석 결과, `waitForCache`가 선점 요청(락 보유자)이 실패했음에도 대기 요청들이 **최대 5초(25회 × 200ms) 폴링을 무조건 채운 뒤**에야 null을 반환하고 FTS로 폴백했기 때문이었습니다. (선점자가 실패하면 캐시가 영영 생성되지 않는데도 불필요하게 5초를 대기)

### 개선 — `waitForCache` Early Exit 도입
- `waitForCache` 폴링 루프 내부에 `lockKey` 존재 여부(`RedisService.exists()`)를 추가로 확인하도록 수정.
- **"락 해제 + 캐시 미생성"** 을 감지하면 최대 대기를 채우지 않고 즉시 null을 반환 → 빠른 FTS 폴백을 유도.

### 결과 (개선 전/후 비교)

| 항목 | 개선 전 | 개선 후 | 개선폭 |
|---|---|---|---|
| 총 요청 수 | 5 | 5 | — |
| **가용성(200)** | **5 (100%)** | **5 (100%)** | 유지 |
| 실패 | 0 | 0 | — |
| 평균 응답시간 | 4.21s | 467.67ms | **약 9배 단축** |
| p90 Latency | — | 511.42ms | |
| **p95 Latency** | **5.17s** | **511.7ms** | **약 10배 단축** |
| **p99 Latency** | 5.17s | 511.93ms | |
| 최대 응답시간 | 5.17s | 511.99ms | |

**서버 로그 확인 (Early Exit 동작 증거)**:
> [AiService] [Embedding Cache MISS] ... (1건, 선점자가 Gemini 호출)
> [AiService] [Embedding Lock Waiting] ... (4건, 대기자)
> [AiService] [embedSearchQuery] 처리 중 에러 발생: API key not valid (선점자 400 에러 즉시 실패 → 락 해제)
> [AiService] [Embedding Lock Released] ... (4건, 대기자가 락 해제 감지 → 즉시 폴백)

→ 에러가 발생했음에도 예외가 클라이언트로 전파되지 않고, 자동으로 PostgreSQL FTS(`searchKeyword`) 경로로 전환되어 정상적으로 키워드 검색 결과 반환.

### 결론
- 외부 AI API가 완전히 장애 상태여도 **검색 API 가용성 100% 유지**.
- `waitForCache`에 **락 해제 감지 기반 Early Exit**를 도입한 결과, 선점 요청 실패 시 대기 요청들이 불필요한 5초 폴링을 하지 않고 즉시 FTS로 폴백하여 **장애 시 응답 지연이 p95 5.17s → 511.7ms로 약 10배 단축**되는 문제 해결 성과를 확인했습니다.

---

## 6. 시나리오 E — Keyword-only(FTS) 베이스라인

### 설계 배경 및 이유
- **설계 이유**: 하이브리드 검색(FTS + pgvector Vector Search) 도입으로 인해 발생하는 연산 오버헤드와 지연 시간을 **단순 키워드 검색(FTS) 베이스라인과 비교하여 정량화**하기 위해 수행했습니다.

### 수행 내용
- Scenario C2와 동일한 Ramping 프로파일(1→50 VUs)로 `searchType=keyword`만 측정.

### 결과 (하이브리드 vs FTS 트레이드오프)

| 지표 | 순수 FTS (E) | 하이브리드 RRF (C2) | 차이 |
|---|---|---|---|
| p95 Latency | 62.74ms | 125.8ms | **약 2.0배 증가** |
| p99 Latency | 84.87ms | 160.4ms | **약 1.9배 증가** |
| 최대 TPS | 621 req/s | 313 req/s | **약 50% 감소** |

### 결론
하이브리드 검색은 순수 FTS보다 응답 지연이 약 2배, 처리량이 약 절반 수준으로 측정되었습니다. 그러나 단순 키워드 매칭이 놓치는 **의미론적 연관 문서(Semantic Context)까지 탐색할 수 있는 검토 가치 대비 허용 가능한 오버헤드 범위**임을 검증했습니다.

---

## 7. 종합 지표 테이블

| 시나리오 | 목적 | 부하 | 성공률 | p90 | p95 | p99 | TPS | 핵심 성과 |
|---|---|---|---|---|---|---|---|---|
| A | Cache Stampede 방지 | 50 VU 동일 키워드 동시 | 100% | 1.03s | 1.03s | 1.04s | 46.3 | 외부 API 호출 **1회로 제한** |
| B | Semaphore(3) 동시성 제어 | 20 VU 다른 키워드 동시 | 100% | 3.07s | 3.35s | 3.38s | 5.86 | 동시 호출 **3개로 제한**, 무장애 처리 |
| B2 | Rate Limit(Throttle) | 동일 IP 100건 | 5%(정상) | 180.67ms | 191.3ms | 329.29ms | — | **95건 정상 차단**, 설계값과 일치 |
| C1 | 하이브리드 Cold 종단 latency | 유니크 30건 단발성 | 100% | 487.02ms | **615.15ms** | **665.53ms** | 7.11 | 실제 외부 API 호출 포함 Cold 지연(캐시 miss 100%) |
| C2 | 하이브리드 순수 DB 성능(warm) | Ramp 1→50 VU | 100% | 114.21ms | **125.8ms** | **160.4ms** | **313.29** | **기본 인프라 수용량 지표** |
| D | AI 장애 FTS 폴백 | 5 VU, Gemini 장애 | 100% | 511.42ms | 511.7ms | 511.93ms | 9.51 | **가용성 100%** + Early Exit로 지연 10배 단축 |
| E | FTS 베이스라인 | Ramp 1→50 VU | 100% | 57.39ms | 62.74ms | 84.87ms | **621.02** | 하이브리드 대비 처리량 비교 기준 |

---

## 8. 테스트 재현 방법

1. 서비스 기동 (WSL2 + Docker Compose)
   `docker compose up -d --build`

2. Throttle 임계값을 테스트용으로 완화 (.env 수정 후 재기동)
   `SEARCH_THROTTLE_LIMIT=1000000` (A/B/C/E 측정 시 사용)
   `docker compose up -d --build backend`

3. cold 캐시가 필요한 시나리오(A/B/C1) 실행 전 임베딩 캐시 초기화
   `docker exec tech-trends-redis redis-cli --scan --pattern "emb:*" | xargs -r -I{} docker exec tech-trends-redis redis-cli DEL {}`

4. k6 실행 (compose 네트워크 내 수행)
   `docker run --rm --network tech-trends_default -v "$PWD/load_test:/scripts:ro" -e BASE_URL=http://tech-trends-nginx/api/trends/search -e SCENARIO=A grafana/k6 run /scripts/search-load-test.js`