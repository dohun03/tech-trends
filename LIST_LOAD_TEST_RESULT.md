# 📊 [성능 개선 보고서] 목록/상세 API k6 부하 테스트 및 최적화

[← README로 돌아가기](./README.md)

---

## 💡 최종 성과 요약

AWS Lightsail 사양(2vCPU / 1GB RAM) 제약 하에서 부하 테스트를 진행하여, **처리량을 3.4배 향상**시키고 **이벤트루프 멈춤(최대 71초) 장애를 완벽 해결**했습니다.

### 🎯 핵심 지표 변화 (High Tier: 50,000건 기준)
* **초당 처리량 (RPS):** `422 req/s` ➔ **`1,447 req/s` (+242.6%)**
* **목록 조회 (List p95):** `250ms` ➔ **`92ms` (-63.2%)**
* **시스템 안정성:** 요청 실패율 **`0%`**, 이벤트루프 스톨 경고 **`211회 ➔ 0회`**
* **운영 환경 검증:** 백그라운드 수집 파이프라인 동시 실행 시에도 **OOM(Out of memory) / 5xx 에러 0건**

---

## 1. 검증 타임라인

| # | 작업 내용 | 성과 및 지표 변화 |
|---|---|---|
| 1 | 정렬 복합 인덱스 3개 추가 | 목록 조회 p95: 250ms ➔ 113ms (-55%) |
| 2 | Docker 1GB / 2CPU 환경 격리 | OOM 발생 여부 검증 (안정적 구동 확인) |
| 3 | 상세/연관 글 조회 Redis 캐싱 & Coalescing 적용 | 단건 응답속도 p95 -75% 이상 개선 |
| 4 | 응답 필드 경량화 & `COUNT(*)` 연산 캐싱 | 처리량 +39% 향상 |
| 5 | Throttler 저장소 Redis 전환 | **71초 이벤트루프 스톨 (211회 ➔ 0회) 완벽 해결** |
| 6 | Redis Fail-Open 폴백 구현 | Redis 중단 시에도 에러율 0% 유지 |
| 7 | Cache / Queue Redis 컨테이너 완전 분리 | eviction 정책 충돌 및 데이터 휘발 위험 제거 |

---

## 2. 핵심 결함 추적 및 해결

### DB 복합 인덱스 적용 및 쿼리 최적화
* **문제:** 데이터가 50,000건으로 증가함에 따라 정렬 쿼리가 전체 테이블을 탐색(Seq Scan, 18.7ms)하여 목록 조회 성능 급격히 저하.
* **해결:** `like_count`, `view_count`, `comment_count` 정렬용 복합 인덱스 3개 추가 및 목록 응답 미사용 필드 제거로 데이터 경량화.
* **결과:** Seq Scan을 Index Scan(0.5ms 이하)으로 전환하여, 데이터량 증가와 무관하게 목록 조회 p95 응답속도를 **92ms**로 안정화.

### 다층 Redis 캐싱 및 요청 병합 적용
* **문제:** 목록(List) 조회시 무거운 `COUNT(*)` 전체 집계 연산과 반복되는 상세/연관/출처(Detail/Related/Sources) 조회로 인해 DB I/O 부하 발생 및 지연시간 악화.
* **해결:** 
  1. `COUNT(*)` 연산을 별도로 분리하고 결과를 Redis에 TTL 3분간 캐싱해 반복 집계 연산 제거.
  2. 상세/연관/출처 데이터 Redis 캐싱 및 동일 자원에 대한 동시 요청을 1회로 묶어주는 **Request Coalescing** 적용.
* **결과:** 단건 응답속도 p95 **75% 이상 개선** 및 전체 **처리량(RPS) +39% 향상**.

### In-Memory Throttler로 인한 잠재적 DoS 취약점 제거
* **문제:** 부하 테스트 중 대량의 유니크 IP(가짜 IP / 로테이션 봇 패턴)가 유입될 때, 기본 `@nestjs/throttler` 인메모리 스토리지에서 IP별 타이머를 메모리에 대량 생성하여 Node.js 이벤트루프가 최대 71초간 멈추는 현상(Stall) 발생.
* **해결:** Rate Limiter 저장소를 인메모리에서 **Redis 기반**으로 교체.
* **결과:** IP 분산 환경에서도 이벤트루프 멈춤 현상이 **211회 ➔ 0회로 완벽 해결**.

### Redis SPOF(단일 장애점) 방지 및 장애 격리 (Fail-Open)
* **문제:** Redis 중단 시 `ioredis`가 명령을 오프라인 큐에 쌓고 계속 재시도하면서 요청이 60초씩 지연되고, 캐시/Throttler용 `allkeys-lru` 정책이 BullMQ(작업 큐)의 유효 데이터를 삭제할 위험 존재.
* **해결:** 
  1. 역할을 분리하여 캐시/Throttler용(`redis-cache`)과 작업 큐용(`redis-queue`)을 컨테이너 수준에서 완전 분리.
  2. Redis 장애 시 캐시를 스킵하고 DB를 직접 조회하도록 **Fail-Open 폴백** 구조 구현.
* **결과:** Redis가 50초간 강제 중단되는 극한 상황에서도 **5xx 에러 및 타임아웃 0건** 달성 및 가용성 검증.

---

## 3. 시나리오별 최종 부하 테스트 결과 (정상 트래픽 모드)

> **테스트 조건:** 4개 컨테이너(Nginx + Backend + Redis Cache + Redis Queue) 합산 1GB RAM / 2vCPU 제한, VU 1 ➔ 50 램핑 (약 18분)

| 데이터 규모 | 처리량 (RPS) | 실패율 | List p95 | Sources p95 | Detail p95 | Related p95 |
|---|---:|---:|---:|---:|---:|---:|
| **Low (1,000건)** | 1,385 req/s | 0% | 77ms | 22ms | 27ms | 28ms |
| **Mid (10,000건)** | 1,351 req/s | 0% | 93ms | 24ms | 26ms | 26ms |
| **High (50,000건)** | 1,447 req/s | 0% | 92ms | 24ms | 24ms | 24ms |

* **인덱스 효과 증명:** 데이터가 1천 건에서 5만 건으로 50배 늘어나도 목록 조회(92ms) 및 단건 조회(24ms, O(1)) 속도가 일정하게 유지됨.

---

## ⚠️ API + BullMQ 수집 파이프라인 동시 실행 결과

API 부하 진행 중 BullMQ 백그라운드 수집 작업을 동시 실행하여 리소스 경합 상황을 테스트함.
* **성과:** 메모리 Peak **163MiB (예산 1GiB의 16%)**, OOM 및 5xx 에러 **0건**으로 안정성 입증.
* **영향:** API 처리량이 **1,447 ➔ 602 req/s로 감소**하고, 상세 조회 p95가 **24 ➔ 68ms로 증가**. 2vCPU를 수집 파이프라인과 나눠 쓰는 것이 유력한 원인으로 추정되나, 정확한 원인은 아직 규명되지 않음(향후 과제).

---

## 📌 향후 과제
* **원격 DB(RDS) 지연 검증:** 현재는 Native Host DB(지연 0ms) 기준이므로, 추후 AWS RDS 이전 시 VPC 네트워크 지연을 포함한 2차 재검증 필요.
* **동시 부하 시 성능 저하 원인 규명:** API+BullMQ 동시 실행 시 처리량·detail p95가 저하되는 원인(CPU 경합 추정)을 프로파일링으로 확정 필요.